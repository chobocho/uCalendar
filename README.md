# uCalendar

![screen_shot](./img/screen_shot.png)


# How to build
- Clone the repository
- Install dependencies with `npm install`
- Install Wails 
  - go install github.com/wailsapp/wails/v2/cmd/wails@latest
- Make project
  - wails init -n uCalendar -t vanilla
- Build with `wails build`
